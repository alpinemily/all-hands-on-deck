import { defineConfig } from 'vite'

export default defineConfig({
  // Change this to match your GitHub repo name when deploying to GitHub Pages
  // e.g. if your repo is github.com/you/all-hands-on-deck → base: '/all-hands-on-deck/'
  base: '/all-hands-on-deck/',
})
